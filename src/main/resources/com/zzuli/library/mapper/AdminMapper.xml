<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzuli.library.mapper.AdminMapper">
    <!--根据管理员账号和密码查询管理员 管理员登录-->
    <select id="getAdminByIdAndPwd" parameterType="admin" resultType="admin">
        select * from admin where admin_id=#{admin_id} and password=#{password}
    </select>

    <!--通过账号获取密码-->
    <select id="getPasswordById" parameterType="admin" resultType="string">
        select password from admin where admin_id = #{admin_id}
    </select>


    <!--修改管理员密码-->
    <update id="updatePassword" parameterType="admin">
        update admin set password = #{password} where admin_id = #{admin_id}
    </update>

</mapper>


